name: frontend
description: A Flutter-based frontend for the dating app
version: 1.0.0+1

environment:
    sdk: ">=2.17.0 <3.0.0"

dependencies:
    flutter:
        sdk: flutter
    http: ^1.2.2

dev_dependencies:
    flutter_test:
        sdk: flutter
